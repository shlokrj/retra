"use client";

export default function Analyze() {
  // TODO: upload -> preview -> POST /predict -> render report + heatmap
  return (
    <main className="min-h-screen">
      <h1>Analyze</h1>

      {/* image uploader */}
      {/* prediction result card (severity + confidence) */}
      {/* original | heatmap overlay */}
      {/* downloadable report */}
    </main>
  );
}
